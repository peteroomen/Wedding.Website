extends ../shared/admin-layout.pug
block head

block content
  div(class="container") 
    br
    h4 Users
    br
    table
      tr
        th Username
        th Guests
        th Personal Message
        th Is Admin
        th Is Ceremony Only
        th

      each user in users
        tr
          td= user.username
          td= user.guests.length > 0 ? user.guests.map((guest) => guest.firstName + ' ' + guest.lastName).join(', ') : 'This user has no guests'
          td= user.message
          td= user.isAdmin ? 'Yes' : 'No'
          td= user.isCeremonyOnly ? 'Yes' : 'No'
          td
            a(href="/admin/users/" + user.id + "/delete" class="btn-flat waves-effect btn-floating right small-margin-left")
              i(class="material-icons dark-text") delete
            a(href="/admin/users/" + user.id  class="btn-flat waves-effect btn-floating right small-margin-left")
              i(class="material-icons dark-text") edit

  a(href="/admin/users/add/" class="btn-floating btn-large waves-effect waves-light fab light-blue accent-3")
    i(class="material-icons white-text") add

